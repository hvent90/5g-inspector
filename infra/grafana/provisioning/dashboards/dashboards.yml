apiVersion: 1

providers:
  - name: 'NetPulse Dashboard'
    orgId: 1
    folder: 'NetPulse'
    folderUid: 'netpulse'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards

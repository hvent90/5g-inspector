# Bun configuration for netpulse monorepo

[install]
# Use exact versions for reproducible builds
exact = true

[install.lockfile]
# Save the lockfile
save = true

[run]
# Silence script output warnings
silent = false
